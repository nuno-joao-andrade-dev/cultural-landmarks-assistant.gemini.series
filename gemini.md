
I want to create a nodejs with react frontend (WITHOUT vite server) project 
- a chatbot with gemini using adk and in js
- use the most recent packages
- create an interface to be used by people interested in Cultural Landmarks to learn about history and culture
- the prompts and all the information should be stored in the indexeddb local storage
- this will be a workshop to use in an university 
- create several folder for each step of the process to allow participants to move from one step to the other easily
- define stages to create this chatbot so the participants can follow easily
- create scripts to run locally and to deploy to cloud run
- create tests for each stage and run the tests to check if everything is working don't forget to run and test the chatbot locally
- use the ADK boiler plate to allow using the agent in localhost directly with adk
- the agent should always return its response formatted as Markdown
- the React frontend must render the AI responses using a Markdown component for proper formatting (headers, lists, links, etc.)
- use model gemini-3.1-flash-lite-preview